export const metadata = {
    title: "All Tasks",
    description: "This is about page",
};

const AlltaskLayout = ({children}) => {
  return (
    <div>
        <div>
            ADD
        </div>
    {children}
    </div>
    
  )
}

export default AlltaskLayout;